import './calendar.css'

export const renderCalendar = () => {
  return `
    <div class="alert alert-primary" role="alert">
      At this moment "Calendar" section is in development phase. Try later.
    </div>
  `
}